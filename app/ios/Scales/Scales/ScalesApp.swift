import SwiftUI

@main
struct ScalesApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .background(Color.background)
                .edgesIgnoringSafeArea(.all)
        }
    }
}
